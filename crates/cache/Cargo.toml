[package]
name = "cache"
version = "0.1.0"
edition = "2024"
description = "A generic, thread-safe, in-memory TTL cache for async Rust"

[dependencies]
tokio = { version = "1", features = ["sync", "time"] }

[dev-dependencies]
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
